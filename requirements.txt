# Phase 0 mock supervisor runs on Python stdlib only.
# Real Claude adapter requires Python >=3.10/3.11 target runtime and:
claude-agent-sdk>=0.1.0; python_version >= "3.10"
textual>=0.50.0
# Optional Phase 2 bridges:
httpx>=0.27.0
pyyaml>=6.0
